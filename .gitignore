# renders: hundreds of MB, regenerable, not source.
# note the /* — excluding "outputs/" itself would make the negation below unreachable
/outputs/*
!/outputs/favorites_page/

# inside the published page, keep the MP3s but never the lossless masters
/outputs/favorites_page/audio/*.flac

.venv/
__pycache__/
*.pyc
.DS_Store

*.safetensors
*.bin
