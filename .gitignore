# renders: hundreds of MB, regenerable, not source
/outputs/

# the published page lives in docs/ because GitHub Pages only serves
# the repository root or /docs from a branch
/docs/audio/*.flac

.venv/
__pycache__/
*.pyc
.DS_Store

*.safetensors
*.bin
